diff --git a/source/m3_exec_defs.h b/source/m3_exec_defs.h
index 99fca1d..0d89333 100644
--- a/source/m3_exec_defs.h
+++ b/source/m3_exec_defs.h
@@ -45,13 +45,21 @@ d_m3BeginExternC
 typedef m3ret_t (vectorcall * IM3Operation) (d_m3OpSig);
 
 #define d_m3RetSig                  static inline m3ret_t vectorcall
 #define d_m3Op(NAME)                M3_NO_UBSAN op_section d_m3RetSig op_##NAME (d_m3OpSig)
 
+#if defined(__clang__) && defined(d_m3AgentOsMusttailDispatch)
+# define M3_MUSTTAIL                __attribute__((musttail))
+#else
+# define M3_MUSTTAIL
+#endif
+
 #define nextOpImpl()                ((IM3Operation)(* _pc))(_pc + 1, d_m3OpArgs)
 #define jumpOpImpl(PC)              ((IM3Operation)(*  PC))( PC + 1, d_m3OpArgs)
 
-#define nextOpDirect()              return nextOpImpl()
-#define jumpOpDirect(PC)            return jumpOpImpl((pc_t)(PC))
+#define nextOpDirect()              M3_MUSTTAIL return nextOpImpl()
+#define jumpOpDirect(PC)            M3_MUSTTAIL return jumpOpImpl((pc_t)(PC))
+#define nextOpNoTail()             return nextOpImpl()
+#define jumpOpNoTail(PC)           return jumpOpImpl((pc_t)(PC))
 
 d_m3EndExternC
 
diff --git a/source/m3_exec.h b/source/m3_exec.h
index 346e7ba..037cdd1 100644
--- a/source/m3_exec.h
+++ b/source/m3_exec.h
@@ -117,7 +117,7 @@ d_m3RetSig  Call  (d_m3OpSig)
         if (UNLIKELY(possible_trap)) return possible_trap;
     }
 
-    nextOpDirect();
+    nextOpNoTail();
 }
 
 // TODO: OK, this needs some explanation here ;0
@@ -543,7 +543,7 @@ d_m3Op  (Call)
     _mem = memory->mallocated;
 
     if (LIKELY(not r))
-        nextOp ();
+        nextOpNoTail ();
     else
     {
         pushBacktraceFrame ();
@@ -582,7 +582,7 @@ d_m3Op  (CallIndirect)
                     _mem = memory->mallocated;
 
                     if (LIKELY(not r))
-                        nextOpDirect ();
+                        nextOpNoTail ();
                     else
                     {
                         pushBacktraceFrame ();
@@ -873,5 +873,5 @@ d_m3Op  (Branch)
 {
-    jumpOp (* _pc);
+    jumpOpNoTail (* _pc);
 }
 
 
@@ -885,9 +885,9 @@ d_m3Op  (If_r)
     pc_t elsePC = immediate (pc_t);
 
     if (condition)
-        nextOp ();
+        nextOpNoTail ();
     else
-        jumpOp (elsePC);
+        jumpOpNoTail (elsePC);
 }
 
 
@@ -898,9 +898,9 @@ d_m3Op  (If_s)
     pc_t elsePC = immediate (pc_t);
 
     if (condition)
-        nextOp ();
+        nextOpNoTail ();
     else
-        jumpOp (elsePC);
+        jumpOpNoTail (elsePC);
 }
 
 
@@ -913,7 +913,7 @@ d_m3Op  (BranchTable)
     if (branchIndex > numTargets)
         branchIndex = numTargets; // the default index
 
-    jumpOp (branches [branchIndex]);
+    jumpOpNoTail (branches [branchIndex]);
 }
 
 
@@ -1141,9 +1141,9 @@ d_m3Op  (BranchIf_r)
 
     if (condition)
     {
-        jumpOp (branch);
+        jumpOpNoTail (branch);
     }
-    else nextOp ();
+    else nextOpNoTail ();
 }
 
 
@@ -1154,9 +1154,9 @@ d_m3Op  (BranchIf_s)
 
     if (condition)
     {
-        jumpOp (branch);
+        jumpOpNoTail (branch);
     }
-    else nextOp ();
+    else nextOpNoTail ();
 }
 
 
@@ -1169,9 +1169,9 @@ d_m3Op  (BranchIfPrologue_r)
     {
         // this is the "prologue" that ends with
         // a plain branch to the actual target
-        nextOp ();
+        nextOpNoTail ();
     }
-    else jumpOp (branch); // jump over the prologue
+    else jumpOpNoTail (branch); // jump over the prologue
 }
 
 
@@ -1182,9 +1182,9 @@ d_m3Op  (BranchIfPrologue_s)
 
     if (condition)
     {
-        nextOp ();
+        nextOpNoTail ();
     }
-    else jumpOp (branch);
+    else jumpOpNoTail (branch);
 }
 
 
@@ -1222,7 +1222,7 @@ d_m3Op  (ContinueLoopIf)
 
         return loopId;
     }
-    else nextOp ();
+    else nextOpNoTail ();
 }
 
 
