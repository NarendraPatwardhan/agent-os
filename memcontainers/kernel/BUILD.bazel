# The kernel — one contract, one implementation on this branch: rust/. The size-optimized
# Zig reimplementation and the parity-gated selector (SYSTEMS.md, B7) are staged on the
# dedicated feature branch and land here only once they pass the parity gate; develop
# carries the Rust kernel alone.
