# spec — GENERATED specs, diff-tested outputs (SYSTEMS.md). Never hand-edited: the
# AsyncAPI/OpenAPI documents and the wire golden-vector corpus are projected from
# contracts/wire.kdl by the projector, and a write_source_files drift gate keeps the
# editor-visible copies honest (B2). The golden vectors pin the wire byte layout across
# Rust and TS so the server and client can't disagree.
#
# Phase A:
#   # //memcontainers/contracts:wire_asyncapi_gen → gen/wire.gen.asyncapi.yaml   (projected)
#   # //memcontainers/contracts:wire_openapi_gen  → gen/wire.gen.openapi.yaml    (projected)
#   # a wire-vector generator     → spec/wire-vectors.json (golden, diff-tested)
